#include "main.h"

/**
 * mul - function that multiplies two integers.
 *
 * @a: first input from user
 * @b: second input from user
 *
 * Return: Always 0 (Success).
 */

int mul(int a, int b)
{
	int mult;

	mult = a * b;
	return (mult);
}
