#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: take no arg, return alwas 0, print a sentense
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
