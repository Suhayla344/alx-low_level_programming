#include <stdio.h>

/**
 * main - Emtry point
 * Desceiption: numbers
 * Return: Always 0 (Success)
 */

int main(void)
{
	int i;

	for (i = 0 ; i < 10 ; i++)
	printf("%d", i);
	printf("\n");
	return (0);
}
