#include <stdio.h>

/**
 * main - Entry point
 * Description: ' I try now to learn landuag C
 * Return: Always 0 (Success)
 */

int main(void)
{
	int i;

	for (i = 97 ; i <= 122 ; i++)
	putchar(i);
	putchar('\n');
	return (0);
}
